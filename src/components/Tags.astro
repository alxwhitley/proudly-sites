---
/*
  Flat hairline pill list describing a project's scope. Styling lives in
  src/styles/global.css (.tags / .tags li). Pass `tags` as a string array.
*/
const { tags = [] } = Astro.props;
---
<ul class="tags">{tags.map((tag) => <li>{tag}</li>)}</ul>
